import Foundation

// Deprecated: tennis-score extraction is now performed upstream by xAI's
// realtime voice agent (see AudioListenerManager.swift). This file is kept
// only so the existing Xcode project file reference still resolves.
// Safe to remove from the Xcode project (right-click → Delete) when
// convenient — no other code references ClaudeService anymore.
